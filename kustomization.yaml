apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Atajo: `kubectl apply -k .` despliega el entorno LOCAL (Minikube)
# Para el server: `kubectl apply -k overlays/server`
resources:
  - overlays/local